/** 원문 강조와 목록 행을 서로 찾아 스크롤하기 위한 id. */
export const markElementId = (candidateId: string) => `masking-mark-${candidateId}`
export const rowElementId = (candidateId: string) => `masking-row-${candidateId}`
